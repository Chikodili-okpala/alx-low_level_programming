#include "main.h"

/**
 * _abs - checks the absolute value of an integer
 * @p: integer to be checked
 *
 * Return: returns the absolute value
 */
int _abs(int p)
{
	if (p < 0)
	{
		return (-p);
	}
	else
	{
	return (p);
	}
}

